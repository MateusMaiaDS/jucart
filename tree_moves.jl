

# # Growing a node
# function grow!(tree::BartTree, tree_residuals::Vector{Float64},bs::BartState,bm::BartModel)

#     leaves::Vector{Node} = get_leaf_nodes(tree.root)
#     index = rand(1:length(leaves))
#     leaf = leaves[index]

#     new_variable = sample_var(bs,bm)
#     new_cutpoint = drawcut()

# end


# # Growing a node
# function grow(tree::Tree, tree_residuals::Vector{Float64},bm::BartModel)

#     leaves::Vector{Node} = get_leaf_nodes(tree.root)
#     index = rand(1:length(leaves))
#     leaf = leaves[index]

#     new_variable = sample_var()
#     new_cutpoint = drawcut()